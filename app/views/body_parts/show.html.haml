.container
  .row
    .col-md-8
      %h1
        = @body_part.name
        Injuries
      %br/
      = render(@injuries) || content_tag(:h5) do
        No injuries submitted
    .col-md-4
      %h4 Can't find the injury you're looking for?
      = link_to "Add Injury", new_body_part_injury_path(@body_part), class: 'btn btn-primary', data: {"no-turbolink" => true}
